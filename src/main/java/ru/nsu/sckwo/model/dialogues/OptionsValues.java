package ru.nsu.sckwo.model.dialogues;

public record OptionsValues(
        int thickness,
        int numberOfFigureVertices,
        int figureRotationAngle,
        int outerRadius,
        int innerRadius
) {
}
